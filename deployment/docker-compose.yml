version: "3.8"

services:
  backend:
    image: ../backend:latest
    platform: linux/amd64
    networks:
      - api.network
  
  frontend:
    image: ../frontend:latest
    platform: linux/amd64
    networks:
      - api.network

networks:
  api.network:
